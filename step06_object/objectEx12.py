import objectEx11

# 아무것도 하지 않아도 objectEx11의 내용이 실행 된다.
# import 호출의 의미이므로 objectEx11의  print(add(1,4)), print(sub(19,4))
# 호출 되어서 실행되는 것이다.

# 그러나 objectEx11의 main이 있으면 실행 되지 않는다.

# main __name__ 는 실행 파일의 이름을 뜻한다.
# 실행 주체가 되면 __main__ 로 변경 된다.
